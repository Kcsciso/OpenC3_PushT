# OpenC3 Push-T (3D 7-DOF) 专家策略与数据集采集系统开发日志

## 一、环境基准与物理真实参数（经 XML / 源码校准）

* **仿真平台**：MuJoCo (30Hz 控制，`timestep=0.002s`，子步 `n_substeps=16`)。


* **执行机构**：OpenC3 7-DOF 机械臂，末端平滑胶囊体推杆（`pusher_tip` 半径 **$R = 8\text{ mm}$**，高空待机 $Z = 0.485\text{ m}$，有效推运高度校准为 **$Z = 0.412\text{ m}$**，消除台面刚性摩擦阻尼）。


* **物理对象**：非凸轻质 T-Block（总重 110g，底面 $Z=0.4005$，顶面 $Z=0.4305$）：


* **横梁 (t_bar)**：半长 `[0.045, 0.015, 0.015]`，相对中心 $[0.0, 0.045, 0.0]$，质量 60g。


* **立柱 (t_stem)**：半长 `[0.015, 0.045, 0.015]`，相对中心 $[0.0, -0.015, 0.0]$，质量 50g。


* **真实物理质心 (COM)**：理论质心位于物块局部坐标系 **$Y = +0.018\text{ m}$**（横梁下沿正下方 12 mm）。




* **验收红线**：质心平面位置误差 **$< 5.0\text{ mm}$**，偏航角残差 **$< 6.0^\circ$**（$0.10\text{ rad}$）。


* **代码仓库**：独立 Git 仓库 `OpenC3_robots`，已同步至 GitHub (`Kcsciso/OpenC3_PushT`)。

---

## 二、架构演进与全周期问题攻坚历史

### V0 ~ V7: 早期原型与几何探索阶段（摘要归档）

* **V0 ~ V2（时钟与接触面缺陷）**：硬编码 170 步截断导致抽搐；水平退刀撞飞物块；改推肩膀切中 15 mm 内凹死角导致方块掀翻。
* **V3 ~ V5（力臂发散与力矩反噬）**：立柱底端推运引发 75 mm 长力臂倒立摆失稳；保底推深 7 mm 引发极限环来回拉锯；独立姿态纠偏违反推力同向公理，导致物块被反向踢远 20 mm。
* **V6 ~ V7（时钟死锁与空划）**：退出 `LIFT` 时 `state_step` 污染导致重规划分支失效（死循环复读第一杆）；25 mm 过大间距叠加伺服滞后导致“直升机式半空推空气”。

---

### V8: 独立大冲程旋转诱发极限环振荡与推远 (Limit-Cycle Oscillation)

* **核心改进**：设立偏角优先拦截逻辑，强制推横梁两翼顶角（$X = \pm 0.038, Y = +0.060$）进行纯旋转微调。


* **暴露问题**：
1. **左右超调对振**：硬编码 18 mm 大冲程在横梁 38 mm 力臂处产生近 $25^\circ$ 角位移，偏角在 $-9.7^\circ \to +6.2^\circ \to -10.9^\circ \to +5.7^\circ$ 间剧烈震荡无法收敛。
2. **单点推运位移累加**：每次旋转单点受力附带向 $-Y$ 移动 10~15 mm，机械臂陷入旋转死循环，将方块从 38.4 mm 生生推远至 75.0 mm。


* **解决对策**：废除远距离独立纯旋转；引入**距离与偏角比例自适应小冲程**（3~7 mm），将单次角位移变化严格抑制在 $2^\circ \sim 3^\circ$ 死区内。

---

### V9: 局部坐标系投影导致推运偏航与倒立摆失稳 (Local Frame Drift)

* **核心改进**：取消远距离纯旋转，恢复纵向推进为主、小步纠偏为辅。
* **暴露问题**：
1. **局部投影右移分量**：当方块存在初始顺时针负偏角（如 $-11.1^\circ$）时，推杆沿局部 $+Y$ 轴向上推进，投影到世界坐标系产生向右分量（$-\sin(-11.1^\circ)\Delta y > 0$），底层轨迹每向上 10 mm 即被强制向右偏移 2 mm。
2. **长力臂失稳正反馈**：推立柱底部（$Y = -0.060$），推力线在质心后方 78 mm 形成持续顺时针力矩，偏角从 $-11.1^\circ \to -17.9^\circ \to -31.2^\circ$ 滚雪球恶化。




* **解决对策**：推运位移向量必须与**世界目标残差矢量**对齐，严禁沿倾斜局部轴推运；大偏角时采用世界绝对负 $Y$ 轴单向轻柔下压。

---

### V10: 双肩差动推进与内凹角“楔子卡死” (Shoulder Push & Armpit Pinch)

* **核心改进**：充分利用 T 方块横梁两翼下沿“肩膀”（$Y = +0.030$），将推力点距质心力臂从 78 mm 骤降至 12 mm，实现“差动推进、边推边正”。


* **暴露问题**：
1. **腋下内凹角卡壳（楔子效应）**：初始将右肩推点选在 $X = +0.022$，推杆半径 $R=8\text{ mm}$，其左外边缘为 $0.022 - 0.008 = 0.014\text{ m}$，直接切入立柱侧壁（$X = +0.015$）内部 1 mm。推杆如楔子挤入直角缝隙，接触力发散直接将方块暴击弹出 35 mm 开外。


2. **近终态暴冲**：距离已达 4.8 mm 临界区时，仍执行 16 mm 冲程，导致临门一脚撞飞物块。


* **解决对策**：
* 将肩膀推点外移至 **$X = \pm 0.032$**，留出整整 **9 mm 侧向绝对净空**，彻底根除夹角干涉。


* 姿态摆正（$\le 2^\circ$）后自动切回立柱底面正中心（$X = 0.0$）对称推进，消除横向偏心矩。


* 引入距离衰减动态冲程（`dist * 0.70 + 0.005`），终态平滑微调至 3~6 mm。



---

### V11: 迟滞回弹与判定阈值失配 (Threshold Hysteresis Mismatch)

* **核心现象**：演示日志输出 `[达标早停] 悬停 1.0 秒成功`，但结尾硬指标结算显示 `最终距目标: 5.3 mm | ❌ FAILED`。


* **根因分析**：状态机达标锁定线设为 4.8 mm，失锁解锁线设为 5.8 mm；推杆抬升时方块惯性轻微回弹至 5.3 mm，仍在迟滞锁定带内触发早停，但被评测硬指标（`dist < 5.0 mm`）直接拦截。


* **解决对策**：收紧收敛阈值——锁定线压缩至 **$< 4.2\text{ mm}$**，解锁线压缩至 **$> 4.8\text{ mm}$**，确保物理回弹上限稳落在 5.0 mm 工业达标红线内。

---

## 三、数据集采集与质量工程规范

为满足 Diffusion Policy 高质量训练需求，数据录制模块（`record_openc3_pusht_dataset.py`）确立以下工程流水线：

* **时钟与时间预算**：单集上限设为 **1500 步（50.0 秒 @ 30Hz）**，确保困难姿态拥有充足收敛时间；引入高精收敛锁死后**悬停 30 步（1.0 秒）原子级早停截断**，消除冗余静止帧干扰。


* **闭环质量过滤与自动重试**：
* 单集结束实时比对终端指标：只有满足 **`dist < 5.0 mm & |yaw| < 6.0°`** 的轨迹才允许写入 Parquet 和视频序列。


* 发生偶发性打滑或超时的回合直接丢弃重试，保证落盘的 40/100 组专家演示 100% 为零瑕疵达标数据。




* **音视频与标量多模态对齐**：通过 `check_openc3_pusht_dataset.py` 自动化检测，确保 Top/Wrist 双目 224×224 视频帧数与 7D 状态/动作 Parquet 行数严格对齐（29,333 帧零丢帧）。


* **桌面端监视同步**：录制全程接入 `mujoco.viewer` 被动视窗，强制维持 30 FPS 时间流速，兼顾后台无损压测与人眼实时监工。



---

## 四、模型训练与系统工程攻坚

### 1. 显存死锁与容器进程隔离 (CUDA OOM & PID Isolation)

* **报错现象**：训练启动瞬间抛出 `CUDA out of memory`，提示 GPU 0 已被占用 35.7 GiB，但 `nvidia-smi` 进程列表为空。
* **物理根因**：Docker / Kasm 容器未挂载 `--pid=host`，容器内部隔离了宿主机进程树；宿主机其他任务霸占了 GPU 0，导致首步卷积分配 14 MiB 失败。
* **工程解决**：
1. 通过环境变量重定向至空闲显卡：`export CUDA_VISIBLE_DEVICES=1`（空闲显存 20.0 GiB）。
2. 启用内存碎片整理：`os.environ["PYTORCH_CUDA_ALLOC_CONF"] = "expandable_segments:True"`。
3. 引入**混合精度与梯度累加**：开启 `torch.amp.autocast(dtype=torch.float16)`；采用 `BATCH_SIZE = 16` + `GRAD_ACCUM_STEPS = 2` 等效 Batch 32 训练，显存峰值从 6.8 GB 压制在 3.4 GB 以内。



### 2. 本体感觉捷径与“盲人假推” (Proprioception Shortcut & Visual Underfitting)

* **现象**：策略模型执行两下推运动作后即在半空划水打空气，完全无视方块当前实际位姿。
* **机理剖析**：
* 在 29,333 帧数据集下，仅跑 15,000 步（Batch 8）等价于全数据集仅遍历 **4.1 个 Epoch**。


* 双路 ResNet18 视觉主干未能从零收敛，网络找到损失下降捷径，完全依赖 7D 关节角（`observation.state`）盲背固定轨迹。




* **工程解决**：
* 加载官方 ImageNet 预训练权重：`pretrained_backbone_weights="ResNet18_Weights.IMAGENET1K_V1"`。
* 将训练步数扩充至 30,000~40,000 步，迫使 CNN 视觉特征充分提取物块 3D 几何特征。



---

## 五、闭环评测失效与物理力学终局攻坚

### 1. LeRobot 内部 FIFO 队列冲突导致段错误 (Conv2d 5D Crash)

* **报错现象**：`RuntimeError: Expected 3D or 4D input to conv2d, but got [4, 2, 3, 224, 224]` 伴随 `Segmentation fault (core dumped)`。
* **技术根因**：LeRobot 的 `policy.select_action` 内部已封装基于 `n_obs_steps=2` 的时序滑窗队列；评测脚本外层用 `deque` 堆叠 4 帧传入，被底层错误解释为 Batch 维度，产生 5 维非法张量摧毁 ResNet 卷积层。


* **工程解决**：评测端严格恢复传入单帧 `(1, C, H, W)` 与 `(1, 7)`，时序状态机完全交由模型底层 FIFO 托管。



### 2. 横向推运偏心力矩甩飞物块 (COM Misalignment on Lateral Push)

* **现象**：机械臂从右向左侧推时，推杆把方块横向挤飞砸偏，无法形成直线推移。
* **物理根因**：
* T 型物块理论质心位于 $Y = +0.018$。


* 早期侧推接触点错误锚定在立柱靠下端（$Y = -0.035$）。


* 作用力与质心存在长达 **53 mm** 的偏心力臂，横向推力产生巨大的偏转力矩，导致立柱被推走而横梁剧烈反向甩尾脱落。




* **终局修复**：将横向侧推接触点严格重构至 **$Y = +0.012$**（距横梁肩部保留 18 mm 净空，距真实质心仅 6 mm），推力线几乎穿透质心，彻底实现横向无力矩稳态平推。



### 3. 时序多模态塌陷与抽搐退刀 (Temporal Ambiguity & Chunking Squeeze)

* **现象**：测试时推杆下潜接触物块后瞬间缩回抬升，动作极度不连贯。
* **机理剖析**：在接触深度 $Z = 0.412$ 处，下探完成（准备 PUSH）与推运完成（准备 LIFT）具有相同的几何高度；在 $T_{\text{obs}}=2$ 极短时序下模型产生多模态塌陷；叠加 `n_action_steps=8`（仅执行 0.26s 即被重新打断重推），推运动作无法连贯展开。


* **工程解决**：
* 评测最大步数拉齐至 **1500 步（50.0 秒）**，保证策略拥有充裕的闭环容错周期。


* 将 `n_action_steps` 扩大至 **12~14 步**，以 Action Chunking 强制机械臂把推运冲程走完，杜绝高频推理抖动。
* 专家录制端在落刀点注入 $\pm 2\text{ mm}$ 随机位置扰动，为模仿学习提供协变量纠偏恢复数据（Anti-Covariate Shift）。





---

## 六、当前各模块参数状态基准表

| 模块 / 环节 | 关键参数项 | 当前调优基准值 | 对应的物理与算法意义 |
| --- | --- | --- | --- |
| **几何与空间** | 推运高度 `z_push` | **$0.412\text{ m}$** | 距桌面留出 11.5 mm 净空，杜绝压台面穿模打滑 |
|  | 双肩推点 $X$ 坐标 | **$\pm 0.032\text{ m}$** | 留出 9 mm 侧向净空，根除 T 梁内凹直角卡壳

 |
|  | 侧推对齐 $Y$ 坐标 | **$+0.012\text{ m}$** | 对齐 $Y=+0.018$ 质心，消除横向推运甩尾力矩

 |
| **专家状态机** | 单轮 FSM 步长配置 | **10 / 15 / 35 / 10** | 巡航/下探/推进/抬升（70 步 = 2.33s/轮，效率提升 35%）

 |
|  | 达标锁定 / 解锁线 | **$< 4.2\text{ mm}$ / $> 4.8\text{ mm}$** | 消除惯性物理回弹，锁死 5.0 mm 工业达标红线 |
| **训练超参数** | 视觉骨干 Weights | **ResNet18_Weights.IMAGENET1K_V1** | 引入预训练先验，彻底击穿“盲人假推”

 |
|  | 批量与步数 | **Batch 16 (Accum 2) / 30k~40k** | 跑满 33+ 个 Epoch，显存压制在 3.4 GB |
| **闭环评测** | 评测上限 / 早停 | **1500 步 (50s) / 连续 30 步** | 给予充分纠偏余量，达标后 1.0 秒利落早停

 |